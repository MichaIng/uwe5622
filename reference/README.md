this folder contains the old conditional patch stack which was used in Armbian before this repository unified everything.  
The `drivers_network.excerpt.sh` shows for which version and which SoC which patch in which order was applied.
